"use client";

/**
 * Inventory page — placeholder until Phase 5.
 * This ensures the /dashboard/inventory route exists.
 */
export default function InventoryPage() {
  return (
    <div className="p-6 max-w-7xl mx-auto">
      <h1
        className="text-3xl font-bold text-foreground tracking-tight mb-2"
        style={{ fontFamily: "Fraunces, Georgia, serif" }}
      >
        Inventory
      </h1>
      <p className="text-text-secondary">
        Your stock management dashboard. Products and controls coming next.
      </p>
    </div>
  );
}
